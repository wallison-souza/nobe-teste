<%= form_with(model: account, local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>
      <ul>
        <% account.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :num_account %>
    <%= form.text_field :num_account %>
  </div>

  <div class="field">
    <%= form.label :num_branch %>
    <%= form.text_field :num_branch %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.text_field :balance %>
  </div>

  <div class="field">
    <%= form.label :inative %>
    <%= form.check_box :inative %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
